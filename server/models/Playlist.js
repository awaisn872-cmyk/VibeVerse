import mongoose from 'mongoose'; const schema=new mongoose.Schema({userId:{type:mongoose.Schema.Types.ObjectId,ref:'User'},name:String,songs:[String]},{timestamps:true}); export default mongoose.model('Playlist',schema);
