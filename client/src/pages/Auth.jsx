import {useState} from 'react'; import {useAuth} from '../context/AuthContext'; import {useNavigate,Link} from 'react-router-dom'; export function Login(){const{login}=useAuth();const nav=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[err,setErr]=useState('');const submit=async e=>{e.preventDefault();try{await login({email,password});nav('/profile')}catch(x){setErr(x.response?.data?.message||'Unable to sign in')}};return <AuthBox title="Welcome back" sub="Sign in and get back to your vibe."><form onSubmit={submit}><input required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="error">{err}</div>}<button className="primary full">Sign in</button></form><p>New here? <Link to="/signup">Create an account</Link></p></AuthBox>}
export function Signup(){const{signup}=useAuth();const nav=useNavigate();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[err,setErr]=useState('');const submit=async e=>{e.preventDefault();try{await signup({name,email,password});nav('/profile')}catch(x){setErr(x.response?.data?.message||'Unable to create account')}};return <AuthBox title="Join VibeVerse" sub="Create your account and make the universe yours."><form onSubmit={submit}><input required placeholder="Your name" value={name} onChange={e=>setName(e.target.value)}/><input required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength="6" type="password" placeholder="Password (6+ characters)" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="error">{err}</div>}<button className="primary full">Create account</button></form><p>Already have an account? <Link to="/login">Sign in</Link></p></AuthBox>}
function AuthBox({title,sub,children}){return <main className="auth-page"><div className="auth-glow"/><div className="auth-box"><Link to="/" className="brand">Vibe<span>Verse</span></Link><h1>{title}</h1><p>{sub}</p>{children}</div></main>}
