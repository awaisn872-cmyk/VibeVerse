import {useEffect,useState} from 'react'; import {api} from '../services/api'; import {artists as fallback} from '../data/fallbackData'; import ArtistCard from '../components/ArtistCard'; export default function Artists(){const[data,setData]=useState(fallback);useEffect(()=>{api.get('/artists').then(r=>setData(r.data)).catch(()=>{})},[]);return <main className="page"><div className="page-title"><span className="kicker">THE CREATIVE MINDS</span><h1>Featured <em>artists</em></h1><p>Meet the voices, writers and performers behind the soundtrack.</p></div><div className="artist-grid">{data.map(a=><ArtistCard key={a.id} artist={a}/>)}</div></main>}
