import {useState} from 'react'; import {movies,music,artists} from '../data/fallbackData'; import MovieCard from '../components/MovieCard'; import MusicCard from '../components/MusicCard'; import ArtistCard from '../components/ArtistCard'; export default function Search(){const[q,setQ]=useState('');const x=q.toLowerCase();const ms=movies.filter(a=>a.title.toLowerCase().includes(x));const songs=music.filter(a=>(a.title+a.artist).toLowerCase().includes(x));const arts=artists.filter(a=>a.name.toLowerCase().includes(x));return <main className="page"><div className="page-title"><span className="kicker">SEARCH</span><h1>Find your <em>next vibe</em></h1><input autoFocus className="big-search" placeholder="Movies, music, artists…" value={q} onChange={e=>setQ(e.target.value)}/></div>{q&&<><h2 className="mini-title">Movies</h2><div className="movie-grid">{ms.map(m=><MovieCard key={m.id} movie={m}/>)}</div><h2 className="mini-title">Music</h2><div className="music-grid">{songs.map(s=><MusicCard key={s.id} song={s}/>)}</div><h2 className="mini-title">Artists</h2><div className="artist-grid">{arts.map(a=><ArtistCard key={a.id} artist={a}/>)}</div></>}</main>}
