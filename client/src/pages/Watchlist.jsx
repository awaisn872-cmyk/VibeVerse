import {movies} from '../data/fallbackData'; import MovieCard from '../components/MovieCard'; import {Bookmark} from 'lucide-react'; export default function Watchlist(){const ids=JSON.parse(localStorage.getItem('vv_watchlist')||'[]');const list=movies.filter(m=>ids.includes(m.id));return <main className="page"><div className="page-title"><span className="kicker"><Bookmark size={13}/> SAVED FOR LATER</span><h1>Your <em>watchlist</em></h1><p>Keep the movies you want to come back to.</p></div>{list.length?<div className="movie-grid">{list.map(m=><MovieCard key={m.id} movie={m}/>)}</div>:<div className="empty"><h2>Your watchlist is empty</h2><p>Save a movie and it will appear here.</p></div>}</main>}
