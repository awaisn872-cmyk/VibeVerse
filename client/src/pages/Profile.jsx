import {useAuth} from '../context/AuthContext'; import {Link} from 'react-router-dom'; import {LogOut,Bookmark,Heart,UserRound} from 'lucide-react'; export default function Profile(){const{user,logout}=useAuth();return <main className="page"><div className="profile-card"><div className="profile-avatar"><UserRound size={44}/></div><span className="kicker">YOUR VIBEVERSE</span><h1>{user?.name}</h1><p>{user?.email}</p><div className="profile-actions"><Link to="/movies" className="ghost"><Bookmark/> Watch movies</Link><Link to="/music" className="ghost"><Heart/> Favorite music</Link><button onClick={logout} className="ghost"><LogOut/> Sign out</button></div></div></main>}
