import {music} from '../data/fallbackData'; import MusicCard from '../components/MusicCard'; import {Heart} from 'lucide-react'; export default function Favorites(){const ids=JSON.parse(localStorage.getItem('vv_favs')||'[]');const list=music.filter(m=>ids.includes(m.id));return <main className="page"><div className="page-title"><span className="kicker"><Heart size={13}/> YOUR FAVORITES</span><h1>Favorite <em>music</em></h1><p>Your saved songs, ready whenever the mood hits.</p></div>{list.length?<div className="music-grid">{list.map(s=><MusicCard key={s.id} song={s}/>)}</div>:<div className="empty"><h2>No favorites yet</h2><p>Tap the heart on any song to save it.</p></div>}</main>}
