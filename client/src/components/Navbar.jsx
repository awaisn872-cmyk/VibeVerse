import {Link,NavLink,useNavigate} from 'react-router-dom'; import {Search,UserRound,LogOut,Menu,X,Play} from 'lucide-react'; import {useState} from 'react'; import {useAuth} from '../context/AuthContext';
export default function Navbar(){const{user,logout}=useAuth();const nav=useNavigate();const[m,setM]=useState(false);return <header className="nav"><Link to="/" className="brand"><span className="brand-mark"><Play size={15} fill="currentColor"/></span>Vibe<span>Verse</span></Link><button className="menu" onClick={()=>setM(!m)}>{m?<X/>:<Menu/>}</button><nav className={m?'open':''}>{[['/','Home'],['/movies','Movies'],['/music','Music'],['/artists','Artists'],['/playlists','Playlists']].map(([to,t])=><NavLink key={to} to={to} onClick={()=>setM(false)}>{t}</NavLink>)}<NavLink to="/search"><Search size={17}/> Search</NavLink></nav><div className="nav-actions">{user?<><Link to="/profile" className="avatar"><UserRound size={18}/>{user.name.split(' ')[0]}</Link><button className="icon-btn" title="Logout" onClick={()=>{logout();nav('/')}}><LogOut size={18}/></button></>:<Link className="login-btn" to="/login">Sign in</Link>}</div></header>}
